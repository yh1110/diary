import { Center, Loading } from "@yamada-ui/react";

export const LoadingPage = () => {
    return (
        <Center>
            <Loading variant="audio" size="6xl" color="amber.500" h="100vh" />
        </Center>
    );
};
